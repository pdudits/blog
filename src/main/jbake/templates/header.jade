html
  head
    meta(charset='utf-8')
    title
      if content.title
        | #{content.title}
      else
        | Patrik Duditš
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(name='description', content='')
    meta(name='author', content='')
    meta(name='keywords', content='')
    meta(name='generator', content='JBake')
    script(src="http://use.edgefonts.net/pt-sans:n4,i4,n7,i7:all;raleway:n1:all.js")

    block scripts
    // Le styles

    link(rel='stylesheet', href='#{content.rootpath}css/app.css')

    // HTML5 shim, for IE6-8 support of HTML5 elements
    // if lt IE 9
      script(src='#{content.rootpath}js/html5shiv.min.js')

    // Fav icon
    link(rel='shortcut icon', href='#{content.rootpath}favicon.ico')
